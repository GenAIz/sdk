// Package env is used to control the exclusion of unsafe configurations from production builds. Configurations which could be useful in other environments, such as dev, may represent a significant risk as production features.
package env
